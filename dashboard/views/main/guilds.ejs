<%- include("../parts/head", {bot, user, path }) %>


  <style>
  
  .servers{
    
   left:0;
    position: static;
    }
      .servers .server{
      margin-right: 100px;
      position:static;
    
      
      
    }

    .servers .server .icon-and-name{
      margin-top:40%;
      display:flex; 
    line-height:15px;
    }
    .servers .server .icon-and-name .server-icon{
    
      width: 50px;
      height:50px;
    }
    .servers .server .icon-and-name .text{
      margin-left:30px;
      font-weight: 600;
      
      color: white;
      
    }
  
  </style>
<div class="has-text-white is-size-6">
  

<h2>SELECT A SERVER</h2>



<% if (!user) { %>
    <div style="width: 512px;
	 padding: 16px;
	 margin: 8px;  background-color: rgba(77, 77, 255, 0.05);">
      <h3 style="color: #4d4dff; 	 margin-top: 0;
	 margin-bottom: 8px;
	 font-size: 18px;
	 font-family: 'Poppins', sans-serif;">Hey <%= user.username%>!</h3>
      <p style="margin: 0;
	 font-size: 14px;
	 font-family: 'Open Sans', sans-serif;
	 color: #555;">Looks like you aren't in partnerbot Support Server. <a href="/dc">Join Here!</a></p>
    </div>

  <% } %>



<div class="servers">
<% user.guilds.forEach(guild => {
  
 const permsOnGuild = new perms(guild.permissions_new);
 if(!permsOnGuild.has("MANAGE_GUILD")) {
 return;
 }
%>
<% if (guild.icon) {
 iconurl = "https://cdn.discordapp.com/icons/" + guild.id + "/" + guild.icon + ".png??size=50";
} else {
 iconurl = "https://cdn.glitch.com/82fe990a-7942-42e3-9790-39807ccdb9f6%2Ficon-404-dark.png?v=1602427904949";
}
%>

 
  <% if(bot.guilds.cache.get(guild.id)) { %>
  <div class="server">
   <div class="icon-and-name">
<img class="server-icon" src="<%= iconurl %>" alt="<% guild.name %>"/>
     
     
    
  <p class="text"><%= guild.name %></p>
  </div>
    <a class="btn btn-success" href="/dashboard/<%=guild.id%>">setting</a>
    
    <a class="edit button" href="/dashboard/setup/<%= guild.id %>"></a>
</div>
  <% } %>

  <% }); %>
  <% user.guilds.forEach(guild => {
 const permsOnGuild = new perms(guild.permissions_new);
 if(!permsOnGuild.has("MANAGE_GUILD")) {
 return;
 }
%>
<% if (guild.icon) {
 iconurl = "https://cdn.discordapp.com/icons/" + guild.id + "/" + guild.icon + ".png??size=50";
} else {
 iconurl = "https://cdn.glitch.com/82fe990a-7942-42e3-9790-39807ccdb9f6%2Ficon-404-dark.png?v=1602427904949";
}
%>

 
  <% if (!bot.guilds.cache.get(guild.id)) { %>
  <div class="server">
   <div class="icon-and-name">
<img class="server-icon" src="<%= iconurl %>" alt="<% guild.name %>"/>
  <p class="text"><%= guild.name %></p>
  </div>
 <a href='https://discord.com/api/oauth2/authorize?client_id=838593240328044554&permissions=67206193&scope=bot&guild_id=<%=guild.id%>' class="add button">Add Bot</a>
</div>
  <% } %>

  <% }); %>
</div>




<script>
    function PopupCenter(e, n, t, i) {
        var o = void 0 != window.screenLeft ? window.screenLeft : screen.left,
            d = void 0 != window.screenTop ? window.screenTop : screen.top,
            c = window.innerWidth ? window.innerWidth : document.documentElement.clientWidth ? document.documentElement.clientWidth : screen.width,
            w = window.innerHeight ? window.innerHeight : document.documentElement.clientHeight ? document.documentElement.clientHeight : screen.height,
            r = c / 2 - t / 2 + o, h = w / 2 - i / 2 + d,
            s = window.open(e, n, "scrollbars=yes, width=" + t + ", height=" + i + ", top=" + h + ", left=" + r);
        window.focus && s.focus()
    }
</script>
<%- include ("../parts/foot.ejs")%>